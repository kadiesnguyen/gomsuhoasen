# Plan: TipTap rich text (done)

1. Install TipTap 3 + isomorphic-dompurify
2. Add `RichTextEditor` + sanitize utils (portal)
3. Wire product form: description, storyContent, hotspot panelContent
4. Showroom: sanitize + render HTML; listing strips tags
5. Typecheck portal + showroom_v2
