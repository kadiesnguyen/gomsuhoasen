# Checkout + Admin Orders — Plan

1. Contracts: `order.ts` + `GHS_CONTROLLERS.ORDER` / `GHS_API.ORDER`
2. Nest `OrderModule` (schema, dto, transitions, public POST, admin list/get/status)
3. Geo JSON → `apps/showroom_v2/public/geo/`
4. Showroom checkout modal on Đặt mua
5. Portal Đơn hàng list + detail (status + confirm)
6. Typecheck + local smoke
